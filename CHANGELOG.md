# SA Modkit changelog

## 0.2.1
- Python catalog reads packs.ini (same file as the EXE).
- GroveCast coords use float_to_integer so Sanny compiles.
- OBS server finds cast.ini after install (parent GroveLink folder).
- GroveCast install copies serve.py and RUN_OBS.bat.
- BUILD_EXE.bat retries without /platform:x86 on old csc.

## 0.2.0
- SA-Modkit.exe via built-in csc.exe. Shared packs.ini.

## 0.1.1
- Packs opt-in.
